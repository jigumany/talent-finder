// This page is intentionally left blank to resolve a Vercel build issue.
// The app's entry point is /dashboard, and routing is handled by the root /src/app/page.tsx.
export default function AppRootPage() {
  return null;
}
